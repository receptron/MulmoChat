// Image models the server uses when a request names none. Keep in sync with
// DEFAULT_GEMINI_IMAGE_MODEL / DEFAULT_OPENAI_IMAGE_MODEL in
// src/config/imageModels.ts (the server cannot import from src/).
export const DEFAULT_GEMINI_IMAGE_MODEL = "gemini-3.1-flash-image";
export const DEFAULT_OPENAI_IMAGE_MODEL = "gpt-image-2.5-flare";
